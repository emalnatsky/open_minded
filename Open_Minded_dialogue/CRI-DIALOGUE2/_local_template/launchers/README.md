# Local Launchers

Put personal launcher scripts here after copying `_local_template` to `_local`.

These files are intentionally not part of the shared code because paths differ per laptop.

Example:

```text
_local/launchers/start_dialogue_stack.bat
```
