# Session Roster Examples

Real roster files contain child identifiers and session details, so they belong in your private `_local\session_rosters` folder and should not be shared.

`example_day.json` is safe to share because it only contains the test children from the ontology test data. Copy it to `_local\session_rosters` when you want a ready-made local test day.

