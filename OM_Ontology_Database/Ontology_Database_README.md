# Quick instruction (Draft version 1) (In Progress)

## What is still not yet implemented:

In config:
- None of the validations are yet working. Keep `SKIP_LLM_VALIDATION = True`. And SHACL 
- Keep `SKIP_API_KEY_CHECK = True` so that the  